<!DOCTYPE html>
<html>

<head>
    <title>Desktop Demo</title>
    <meta name="viewport" content="width = device-width, initial-scale = 1">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.3/css/materialize.min.css">
    <script type="text/javascript" src="https://code.jquery.com/jquery-2.1.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.97.3/js/materialize.min.js"></script>
    <script type="text/javascript" src="assets/js/jquery.sticky.js"></script>
    <script>
        $(document).ready(function () {
            $('#sticker').sticky({ bottomSpacing: 0 });
            $('#sticker').on('sticky-start', function () { console.log("Started"); });
            $('#sticker').on('sticky-end', function () { console.log("Ended"); });
            $('#sticker').on('sticky-update', function () { console.log("Update"); });
            $('#sticker').on('sticky-bottom-reached', function () { console.log("Bottom reached"); });
            $('#sticker').on('sticky-bottom-unreached', function () { console.log("Bottom unreached"); });
        });

    </script>
    <style>
        body {
            height: 800px;
        }

        .pdp-header {
            height: 50px;
            background-color: #F5F5F5;
            width: 100%;
        }

        .shop {
            font-family: sans-serif;
            font-size: 17px;
            font-weight: 700;
            display: flex;
            color: #2A2A2A;

        }

        .shop-text {
            margin-top: 16px;
        }

        .icons {
            display: flex;
            margin-left: 95%;
            margin-top: -28px;
            color: #2A2A2A;
        }

        .back-icon {
            margin-right: 20px;
            margin-top: 16px;
            color: #2A2A2A;
        }

        .image-gallery {
            height: 400px;
        }

        .skuset {
            height: 100px;
            background-color: #FAFAFA;
            border-bottom: 2px solid #E8E8E8;
            width: 100%;
        }

        .merchants-grid {
            height: 300px;
            background-color: #FAFAFA;

        }

        .merchants-grid-title {
            width: 100%;
            padding: 10px;
            font-family: sans-serif;
            font-size: 17px;
            font-weight: 500;
            color: #747474;
            padding-top: 20px;
        }

        .pd-grid {
            background-color: #FAFAFA;
            height: 800px;
        }

        .title {
            height: 200px;
            border-bottom: 2px solid #E8E8E8;
        }

        .title-text,
        .product-price {
            width: 100%;
            padding: 10px;
            font-family: sans-serif;
            font-size: 20px;
            font-weight: 700;
            margin-top: 20px;
        }

        .price-alert-btn,
        .share-btn {
            background-color: #F5F5F5;
            margin-right: 20px;
            margin-top: 20px;
        }

        .icon-color {
            color: #757575;
        }

        .description-grid {
            height: 270px;
            border-bottom: 2px solid #E8E8E8;
        }

        .description-title {
            width: 100%;
            padding: 10px;
            font-family: sans-serif;
            font-size: 20px;
            font-weight: 700;
            margin-top: 20px;
        }

        .description-text {
            width: 100%;
            font-family: sans-serif;
            font-size: 17px;
            font-weight: 500;
            color: #6f6f6f;
        }

        .read-more {
            width: 100%;
            padding: 10px;
            font-family: sans-serif;
            font-size: 20px;
            font-weight: 600;
            margin-top: 5px;
            margin-left: 85%;
        }

        .details-grid {
            width: 100%;
            height: 170px;
            margin-top: 1px;
        }

        .brand-grid,
        .discount-grid,
        .division-grid,
        .category-grid {
            border: 1px solid #A2A2A2;
            height: 113px;
            width: 25%;
        }

        .row .col .s2 {
            width: 25%;
        }

        .row .col.s5 {
            padding: 0;
            border-right: 2px solid #D9D9D9;

        }

        .brand-title,
        .discount-title,
        .division-title,
        .category-title {
            margin-top: 20px;
            font-size: 18px;
            font-family: sans-serif;
            width: 100%;
            margin-left: 10px;
            font-weight: 300;
            color: #6f6f6f;
        }

        .brand-name,
        .discount-name,
        .division-name,
        .category-name {
            font-size: 18px;
            font-family: sans-serif;
            width: 100%;
            margin-left: 10px;
            font-weight: 700;
            color: #626262;
        }

        .footer-grid {
            height: 85px;
        }

        .footer-text {
            font-size: 15px;
            font-family: sans-serif;
            margin-left: 10px;
            font-weight: 700;
            color: #626262;
            margin-top: 20px;
            padding-top: 20px;
        }

        .size,
        .color {
            width: 100%;
            padding: 10px;
            font-family: sans-serif;
            color: #747474;
            font-size: 15px;
            font-weight: 500;
            margin-top: 5px;
        }
    </style>
</head>

<body>
    <div class="row">
        <div class="pdp-header">
            <div class="shop">
                <i class="material-icons back-icon">arrow_back</i>
                <span class="shop-text">Shop on L'oreal</span>
            </div>
            <div class="icons">
                <i class="material-icons">shopping_cart</i>
                <i class="material-icons">more_vert</i>
            </div>
        </div>
        <div class="col s5">
            <div class="image-gallery"></div>
            <div class="skuset">
                <div class="size">
                    Size
                </div>
                <div class="color">Color</div>
            </div>
            <div class="merchants-grid">
                <div class="merchants-grid-title">Buy Now</div>
            </div>
        </div>
        <div class="col s7 pd-grid">
            <div class="title">
                <div class="title-text">${input.titleText}</div>
                <div class="product-price">${input.productPrice}</div>
                <a class="btn price-alert-btn icon-color">
                    <i class="material-icons left icon-color">add_alert</i>
                    ADD PRICE ALERT
                </a>
                <a class="btn share-btn icon-color">
                    <i class="material-icons left icon-color">share</i>
                    SHARE
                </a>
            </div>
            <div class="description-grid">
                <div class="description-title">Description</div>
                <div class="description-text">${input.descriptionText}</div>
                <div class="read-more"> Read More</div>
            </div>
            <div class="details-grid">
                <div class="col s2 brand-grid">
                    <div class="brand-title">Brand</div>
                    <div class="brand-name">${input.brandName}</div>
                </div>
                <div class="col s2 discount-grid">
                    <div class="brand-title">True Discount</div>
                    <div class="brand-name">${input.discount}</div>
                </div>
                <div class="col s2 division-grid">
                    <div class="brand-title">Division</div>
                    <div class="brand-name">${input.division}</div>
                </div>
                <div class="col s2 category-grid">
                    <div class="brand-title">Category</div>
                    <div class="brand-name">${input.category}</div>
                </div>
            </div>
            <div class="footer-grid z-depth-2">
                <div class="icon"></div>
                <div class="footer-text">${input.footerText}</div>
            </div>
        </div>
    </div>
</body>

</html>